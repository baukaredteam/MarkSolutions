# Cursor factory (local)

Плагины не живут в git. Человек ставит в Cursor Ultra:

- `/add-plugin compound-engineering`
- После Node: `npx skills add tt-a1i/archify` и `npx skills@latest add mattpocock/skills`

Grok Bot использует ту же библиотеку плагинов аккаунта Cursor. Не клонировать plugin repo на VM бота.

Issue tracker / triage / domain: `issue-tracker.md`, `triage-labels.md`, `domain.md`.
