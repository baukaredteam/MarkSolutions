# Review T2 постфактум (e0c5967)

Дата: 2026-08-06. Фокус: api-client Bearer, маппинг ошибок, /products ApiUnavailable, /apply AT-02.

## Критичные — исправлено в коммите

| #   | Severity | Замечание                                                                                                                                                                                                                            | Статус                                                                                                                                                          |
| --- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | MEDIUM   | /apply: дубль БИН. T1-бэкенд возвращает **200** (не 409) для существующей заявки; старый код ждал 409 → тост «Заявка отправлена» вместо «Заявка уже существует». Кнопка блокировалась (setDone), но сообщение вводило в заблуждение. | Исправлено: api-client.postRaw возвращает `{status, body}`; apply различает 200 (дубль → «Заявка уже существует. Статус: …») от 201 (новая). Ветка 409 удалена. |
| 2   | MEDIUM   | /products: тост «400: tenant_id required» — устаревший контракт T0-web (guard возвращал 400 по x-tenant-id). После T1 guard даёт 401 «jwt required».                                                                                 | Исправлено: тост «401: jwt required».                                                                                                                           |
| 3   | LOW      | Истечение JWT не обрабатывается: при 401 api-client бросает ApiErrorResponse, страницы показывают тост, но нет авторедиректа на /login и очистки сессии.                                                                             | Оставлено (prototype): /login и sessionStore.clear() есть; авторедирект на 401 — задача при появлении router-guard в след. итерации.                            |

## Пункты без замечаний (✓)

| #   | Статус                                                                                                                                                                   |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | ✓ api-client берёт Bearer из localStorage (`sessionStore.get().token`); токен НЕ в URL (fetch path, не query); токен НЕ логируется (нет console.log с token); Accept:_/_ |
| 2   | ✓ маппинг Приложения B: ApiErrorResponse{code,message,details,fieldErrors,correlationId,retryable} → тост `${code}: ${message}`; stack не показывается                   |
| 3   | ✓ /products переживает ApiUnavailable: локальная фикстура, сетевых вызовов нет; без сессии — тост, не белый экран                                                        |
| 4   | ✓ /apply блокирует повторный POST после любого ответа (`done` flag, кнопка disabled); AT-02: дубль не создаёт вторую заявку (бэкенд, count=1)                            |

## Верификация

- npm test: 10 файлов, 30 тестов ✓
- npm run typecheck: exit 0 ✓
- npm run lint: exit 0 ✓
- npm run secret-scan: exit 0 ✓
