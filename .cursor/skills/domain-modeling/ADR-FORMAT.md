# ADR Format

Prefer existing `docs/DECISIONS.md` unless the user asks for a numbered ADR file.

If creating `docs/adr/NNNN-slug.md`:

```md
# {Short title}

{1-3 sentences: context, decision, why.}
```

Offer an ADR only if: hard to reverse, surprising without context, real trade-off.
